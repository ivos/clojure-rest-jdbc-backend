(ns clojure-rest-jdbc-backend.core-test
  (:require [clojure.test :refer :all]
            [clojure-rest-jdbc-backend.app :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
